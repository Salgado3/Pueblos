<%- include('partials/header') -%>

<link rel="stylesheet" href="/css/landingAndAbout.css" />
<div class="container">
  <div class="row mt-5 profile">
    <div class="col-3">
      <div>
        <a href="/logout" class="btn btn-primary btn-lg">Logout</a>
      
          <p class="userInfo"><strong>User Name</strong>: <%= user.userName %></p>
          <p class="userInfo"><strong>Email</strong>: <%= user.email %></p>
          
      </div>
  </div>
      <div class="col-6">
        <ul class="row list-unstyled">
          
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="col-6 justify-content-between mt-5">
            <div class="profileImg">
            <a href="/post/<%= posts[i]._id%>">
              <img class="img-fluid" src="<%= posts[i].image%>">
            </a>
          </div>
          </li>
        <% } %>
        </ul>
        <div class="row justify-content-center mt-5">
          <a class="btn btn-primary" href="/feed">Return to Search</a>
        </div>  
      </div>  
    </div>
  </div>
</div>
<%- include('partials/footer') -%>